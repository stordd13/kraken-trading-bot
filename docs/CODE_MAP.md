# CODE_MAP — où est quoi dans KrakenBot

> **Mesure SOL/D2 (mergé le 2026-09-25, commit de merge de `feat/c3b-sol-d2`) — régénération relancée : sortie identique**, aucun fichier compté n'a changé depuis `98b3e50` (`src/` et `scripts/*.py` inchangés ; src 29 588 / scripts 16 490 lignes, comme ci-dessous) — tout le chantier vit dans `scripts/audit/warmup_at.py` (786 lignes, hors comptage), `tests/test_scripts/test_warmup_at.py` (93 tests, hors comptage) et `results/sol_d2_1w_modes/`. Ligne `audit/warmup_at.py` dans la table `scripts/` ci-dessous.
> **Reconstruction des 8 estampilles 1 w USDT (branche `feat/c3b-reconstruction-1w`, 2026-09-24 — régénéré sur la branche, avant le merge)** : seul changement de `src/` : `models/market_data.py` (+95 / −1 : nouveau modèle `OHLCDerived:171`, table de provenance `ohlc_derived` — une row ⟺ la row `market_data_ohlc` de même clé est dérivée, pas observée ; révision Alembic `c3bd1e7a0001`, **appliquée sur la base de production par le tunnel le 2026-09-24**) et `models/__init__.py` (export) ; `scripts/*.py` inchangés (le chantier vit dans `scripts/audit/reconstruct_1w.py`, 1 959 lignes, hors comptage, et `tests/test_scripts/test_reconstruct_1w.py`, 56 tests) ; src 29 588 lignes (+95), scripts 16 490. Rapport : `results/reconstruction_1w_2022_2025/report.md`.
> **Protocole C3 v2.1 (mergé le 2026-09-23, `b3524ac`, tag `v2.12.0-c3-v2.1` sur le merge) — régénération relancée : sortie identique**, aucun fichier compté n'a changé depuis `f321ddd` (`git diff --stat f321ddd HEAD -- src scripts/*.py` vide ; src 29 493 / scripts 16 490 lignes, comme ci-dessous) — tout le chantier vit dans `scripts/audit/c3_*.py` (7 modules, 7 045 lignes, hors comptage : `c3_common` porte désormais le rejeu du § F.2 et l'admission de l'évaluation), `tests/test_scripts/test_c3_*.py` (8 fichiers, 932 tests, hors comptage), `docs/protocole_c3.md` (**v2.1**, sha256 `9300f4e5…4129`), `docs/amendements_c3_v2.1.md` et `docs/CONTRAINTES_POST_B4.md` § 10. Ligne `audit/c3_*.py` dans la table `scripts/` ci-dessous. Les sites de la **dette 19** sont inchangés.
> **Inventaire de données (mergé le 2026-09-23, `8fdaa2a`) — régénération relancée le 2026-09-23 : sortie identique**, aucun fichier compté n'a changé depuis `ad3b1b1` (`git diff --stat ad3b1b1 HEAD -- src scripts/*.py` vide ; src 29 493 / scripts 16 490 lignes, comme ci-dessous) — tout le chantier vit dans `scripts/audit/data_inventory.py` (1 143 lignes, hors comptage ; **1 207 après `--pairs`, chore/data-binance-usdt 2026-09-23**), `tests/test_scripts/test_data_inventory.py` (29 tests, hors comptage ; **39** après `--pairs`) et `results/data_inventory_20260923/` (`inventory.json` + `inventory.md`, faits sans recommandation). Ligne `audit/data_inventory.py` dans la table `scripts/` ci-dessous.
> **C3a (mergé le 2026-09-23, `64adede`, tag `v2.11.0-c3a-protocole` sur le commit de régénération) — régénération relancée : sortie identique**, aucun fichier compté n'a changé depuis `v2.10.0-c2-replay` (`git diff --stat v2.10.0-c2-replay HEAD -- src scripts/*.py` vide ; src 29 493 / scripts 16 490 lignes, comme ci-dessous) — porte pré-merge § L.5 passée le 2026-09-22 au `6f7ed8e` (24/24 déterminisme serveur, `results/c3a_determinism_server/run_6f7ed8e/`, pilotes versionnés), rapport `agent/rapport_session_c3a_20260922.md` § 14 — tout le chantier vit dans `scripts/audit/c3_*.py` (7 modules, 6 489 lignes, hors comptage), `tests/test_scripts/test_c3_*.py` (8 fichiers, 806 tests, hors comptage) et `results/c3a_entry_validation/` (5 artefacts). Spécification : `docs/protocole_c3.md` **gelé** au `d931293` ; comportement et conventions datées : `skills/backtest.md` § « Validation C3 ». Ligne `audit/c3_*.py` dans la table `scripts/` ci-dessous. Les sites de la **dette 19** (`BacktestEngine` sans liquidation terminale, `backtest.py:1555-1561`, `:3267`) sont inchangés — prérequis C3b.
> Régénéré le 2026-09-19 après le merge C2 (`dev`, tag `v2.10.0-c2-replay` sur le commit de régénération). **Régénération relancée le 2026-09-20 après le rejeu diagnostic grid : sortie identique**, aucun fichier compté n'a changé — tout le chantier vit dans `scripts/audit/` (hors comptage) et `tests/` (hors comptage) ; seul le statut de la migration `c1ae7a1c0001` est corrigé ci-dessous (elle est appliquée sur le serveur, la ligne du tableau était en retard sur l'en-tête). Générations précédentes : 2026-09-15 (post-B4.3, tag `v2.8.0-b4-3-campaign`) — C1 n'avait pas été régénéré, sa ligne ci-dessous rattrape ce retard.
> **C2 (mergé le 2026-09-19, `7414c49`, tag `v2.10.0-c2-replay`)** : nouveau `src/krakenbot/replay_contract.py` (67 lignes — `REPLAY_VERSION = 2`, `ReplayVersionError:35`, `entry_replay_version:39`, `require_replay_version:45` : version de **ce que les moteurs simulent**, pendant du `metrics_version` de C1 ; clé au niveau supérieur d'une entrée de résultats, jamais dans `BacktestMetrics.to_dict()` ; module pur, aucun import de `scripts/`) ; `strategies/grok_grid_atr_adaptive_v4.py` (+92 : nouveau `_match_sell_fill:704` — un SELL ferme le lot qu'il désigne, par `position_id` ou par égalité exacte `sell_level == price`, fin du match par proximité `< 1 USD` (double pop SOL de B4, dette 14) ; compteurs `fill_anomalies` (`unmatched_position_id`, `unmatched_sell_fills`, `ambiguous_sell_fill`, `incoherent_sell_fill`) exposés par `get_state`, `_emit_grid_signal:543` prend `amount_btc` pour vendre le lot dans sa propre quantité) est le seul autre changement de `src/` — et il change **aussi le chemin live/paper** : un fill non attribuable n'est plus attribué arbitrairement, il est journalisé et compté (réconciliation = dette 13 élargie) ; `scripts/backtest.py` (+741 lignes : `RejectionLedger:237` (comptage par (ordre distinct, cause) — `note:266`, `prune:276`, `add_counts:281`, `summary:287`), `IndicatorRequirement:313` + `indicator_requirements:378` + `preregister_indicators:452` + `warmup_needs:464` (R2 : pré-enregistrement des indicateurs lazy **aux paramètres effectifs**, warmup dimensionné en bougies et non en calendrier), `load_context_series:472` / `_load_candles_before:534` (extension arrière `_WARMUP_EXTENSION_FACTOR = 3`, tolérance de trou 1, rapport par TF via `warmup_summary:1020`/`:3078`), `ReplayEvent:565` + `_GRID_REPLAY_PHASE` + `GridBacktester._replay_grok_grid:2957` (R1 : décisions du grid sur les **vraies clôtures 4h**, exécution sur les bougies de trading, ordre intra-timestamp exec → 1w → 1d → 4h), `_order_id_for_level:2488`, `_process_grok_grid_sell_fill:2592` valide le lot **avant** toute mutation de solde (`unmatched_position_id`, `insufficient_inventory` avec dust prec-28 ; pré-C2 le wallet était débité puis le fill abandonné — dette 15(d)), `rejections_summary:1992`/`:3304`, `dca_counters_summary:1996`/`:3310`, `_merge_strategy_anomalies:3314`, `_print_replay_blocks:3693`, gardes CLI grid dans `parse_args:3658` (`--cross-validate` refusé, `--interval` < 4h exigé)), `run_p7_grid_search.py` (+39) / `run_p6_backtests.py` (+33) (`replay_version` écrit au niveau supérieur de chaque entrée, `ReplayVersionMismatchError` au resume **et** au seeding de la phase 2, blocs `rejections` / `warmup` / `dca_counters` persistés par segment), `run_p6_walkforward.py` (+18 : `run_single_backtest` renvoie 4 valeurs, refus d'un survivant pré-C2), `filter_p6_survivors.py` / `generate_p6_report.py` / `p7_report.py` (+6 chacun : `require_replay_version` — un fichier `metrics_version` 2 doit aussi être d'un seul contrat de rejeu, le chemin legacy v1 des fichiers B4 gelés reste intact) ; `scripts/audit/c1_equity_probe.py` (+85, hors comptage : `compare-ab --strict`, `source_fingerprints`, additions tolérées `replay_version`/`rejections`/`warmup`/`dca_counters`). **Conséquence** : tout fichier P6 / P7 / B4 / C1 antérieur est pré-C2 et ne peut être ni repris, ni agrégé, ni classé avec du `replay_version` 2 — il faut réécrire dans un `--output` neuf. `config/`, `core/`, `connectors/`, `execution/`, `indicators/`, `models/`, le collector et le reste du runtime sont inchangés. Lignes des 9 modules touchés régénérées ci-dessous, les autres modules sont inchangés.
> **C1 (mergé le 2026-09-16, `51be3e8`, tag `v2.9.0-c1-metrics` — CODE_MAP non régénéré à l'époque)** : nouveau `src/krakenbot/backtest_metrics.py` (426 lignes, **système de mesure unique** des deux moteurs et des benchmarks : `METRICS_VERSION = 2`, `EquityPoint:64`, `ExternalFlow:75`, `TradeLeg:83`, `DailySeries:95`, `MetricsVersionError:119`, `daily_grid:128`, `resample_daily:144`, `sharpe_ratio:207`, `sortino_ratio:220`, `max_drawdown_pct:231`, `cagr_pct:245`, `calmar_ratio:252`, `ProfitFactorSummary:264`, `net_trade_pnls:277`, `profit_factor_from_sums:293`, `MetricsResult:314`, `compute_metrics:330`, `mean_available:376`, `entry_metrics_version:384`, `require_metrics_version:396`, `fmt:417` — rééchantillonnage journalier UTC, flux externes hors rendement, index de performance, ratio indéfini = `None` jamais 0, PF net des deux jambes ; Python pur, Decimal en entrée, aucun import de `scripts/`) ; `models/trades.py` (+24 : `BacktestRun.sharpe_ratio` / `sortino_ratio` / `profit_factor` deviennent nullable — un ratio indéfini est NULL, pas 0 —, nouvelles colonnes `gross_profit_net`, `gross_loss_net`, `pf_excluded_trades`, `metrics_version` ; révision Alembic `c1ae7a1c0001`, appliquée sur le serveur) est le seul autre changement de `src/` ; `scripts/backtest.py` (+151 lignes : `BacktestMetrics` porte `max_drawdown_pct_daily` (sélection) et `max_drawdown_pct_engine` (diagnostic) au lieu de `max_drawdown_pct`, ratios `None` quand indéfinis, `equity_daily`, `n_daily_returns`, `equity_daily_dict:708`, `profit_factor_display:713` ; nouveaux `_side_str:727`, `_buy_fee_from_cost_basis:731`, `apply_shared_metrics:738` (les deux moteurs délèguent au module partagé en fin de run, l'état de simulation n'est jamais touché), `_db_ratio:3713`, `dump_equity_jsonl:3720` + option `--equity-out` ; `BacktestTrade.buy_fee_alloc`), `p7_report.py` (+336 : agrégation None-aware, PF OOS reconstruit sur les sommes `gross_profit_net` / `gross_loss_net` au lieu d'une moyenne de ratios, `pf_excluded_trades_oos`, chemin **legacy v1** conservé tel quel pour les fichiers pré-C1 (campagne B4 gelée), `--benchmarks` v2 obligatoire dès `metrics_version` 2), `run_p7_grid_search.py` (+114) / `run_p6_backtests.py` (+79) (`metrics_version` écrit dans chaque entrée, `MetricsVersionMismatchError` au resume, `assert_homogeneous` couvre tout le fichier cible, `equity_daily` persisté par segment), `dashboard.py` (+46 : `_is_missing:673`, `_ratio_display:677`, `_c1_label:684`, `_profit_factor_display:691` — un ratio NULL s'affiche `n/a`, le PF affiche ∞ / n/a / incomplet depuis les sommes stockées, et un PF pré-C1 est étiqueté `(v1)` pour ne jamais être appelé net), `filter_p6_survivors.py` (+45) / `generate_p6_report.py` (+39) (`require_metrics_version`, Sortino et Calmar dans les critères et les tableaux, métrique indéfinie = critère échoué avec motif explicite, benchmarks v1 refusés), `run_p6_walkforward.py` (+32), `compute_benchmarks.py` (+1 net : `compute_risk_metrics:69` réécrite sur le module partagé — la maison Sharpe/Sortino/MaxDD/Calmar disparaît —, le DCA fixe tient un compte cash et comptabilise chaque dépôt hebdomadaire en **flux externe**, ce qui corrige le défaut D6 de l'audit red-team B4, les dépôts n'étant plus comptés comme des rendements et le MaxDD n'étant plus l'artefact à 100 %) ; nouveau `scripts/audit/c1_equity_probe.py` et `scripts/backup_db.sh` (`pg_dump` via `docker exec`), hors comptage. **La simulation ne change pas** : aucun moteur ne modifie une décision ni un fill, identité A/B prouvée (`results/C1_metrics_report.md`, `results/c1_ab/`). Lignes des 11 modules touchés régénérées ci-dessous, les autres modules sont inchangés.
> **B4.3 (mergé le 2026-09-15, `4c98b6b`)** : **`src/` inchangé** (aucun fichier, aucune ligne — collector et runtime en parité sans restart) ; `scripts/backtest.py` (+305 lignes : `GridBacktester` liquidation terminale atteignable sur le chemin grok — `_record_equity`, `_liquidate_lot`, `_force_close_open_positions` réécrite, `liquidation_summary` —, `net_pnl` = fees comptées une fois dans les deux moteurs, `capture_effective_params`, `min_order_usdc`, `pair_costs` au site de liquidation), `run_p6_backtests.py` / `run_p6_walkforward.py` / `run_p7_grid_search.py` (`--pair-costs-file`, `--min-order-usdc`, `CampaignConfigMismatchError`, blocs `liquidation` + `effective_params` persistés, `--benchmarks`/`--report`/`--selection`, `load_benchmark_details`), `p7_grids.py` (spacing `[0.015, 0.020, 0.025, 0.030]`), `p7_report.py` (règle GO P7 n° 1 : `ineligible_configs`, `ineligible_flagged` ; trades à côté des métriques, table benchmarks return/MaxDD + note DCA), nouveau `scripts/b4_flags.py` (règle de flag), `filter_p6_survivors.py` / `generate_p6_report.py` / `compute_benchmarks.py` (chemins en arguments, `--fees` + coûts sur les benchmarks, section flags) ; nouveaux `scripts/audit/b4_3_gate_a_reconcile.py`, `b4_p6_checkpoint.py`, `b4_p7_checkpoint.py` (hors comptage). Lignes des 10 scripts touchés régénérées ci-dessous, les autres modules sont inchangés.
> **B4.2 (mergé le 2026-09-14, `3406a6c`)** : `config/settings.py` (+`FEE_MODEL_NAMES`, `ExchangeFees.from_name`, docstrings) est le seul changement de `src/` ; `scripts/backtest.py` (−255 lignes de chemins morts, `--fees` obligatoire, `fee_model` requis, champs d'audit de `BacktestTrade`, `PairCosts`, `--trades-out`, parser module), `run_p6_backtests.py`, `run_p7_grid_search.py`, `run_p6_walkforward.py` (nouvelle CLI), `binance_vision_import.py` (`.env` dans `main()`), `dashboard.py` (`DASHBOARD_FEE_MODEL`), nouveau `scripts/audit/b4_2_reference_capture.py` (hors comptage) ; `tests/conftest.py` (purge credentials + boucle d'événements de session). Lignes des 7 modules touchés régénérées ci-dessous, les autres modules sont inchangés.
> **B4.1 (mergé le 2026-09-13, `c67daeb`)** : `src/` ne change que par des docstrings (`models/market_data.py` commentaire `timestamp` = fin de période, `data/backfill.py`, `connectors/exchange.py`) ; révision Alembic `b4c0ffee0001` (`COMMENT ON COLUMN` seul, appliquée sur le serveur) ; `scripts/binance_vision_import.py:parse_klines_csv` écrit `open_time + interval` ; nouveaux `scripts/audit/b4_stamp_lib.py`, `b4_timestamp_audit.py`, `b4_restamp_binance.py` (hors comptage, voir `results/B4_1_timestamp_restamp_report.md`). Lignes des 4 fichiers touchés régénérées ci-dessous, les autres modules sont inchangés.
> Depuis la génération post-B2 ont changé : `data/backfill.py` (nouveau), `scheduler/task_scheduler.py`, `collector.py`, `connectors/exchange.py`, `connectors/bybit/rest.py`, `core/event_bus.py`, `config/settings.py`, `indicators/multi_timeframe.py`, `multi_pair_registry.py`, `utils/time_utils.py`, `connectors/{bybit,binance}/ws.py` (1 kwarg de log), scripts `bybit_kline_import.py` + `backfill_gap.py` (nouveaux), `fetch_ohlc.py` + `backfill_binance_gap.py` (supprimés)
> (`git diff --stat v2.4.0-b2-bybit-ws..HEAD -- src scripts`) : leurs lignes sont à jour, les autres modules sont inchangés.
> Commande : `wc -l` + `grep -n -E "^(class |def |async def )|^    (async )?def [a-z]"` + `grep -n "^from krakenbot"` sur `src/krakenbot/**/*.py` et `scripts/*.py`.
> À régénérer à chaque merge sur `dev`. Numéros de ligne = `symbole:ligne`. Hors tests, hors `scripts/audit/`.
> Total : src 29 588 lignes (**+95 en reconstruction 1 w** = `models/market_data.py` +94 net, `models/__init__.py` +1 net ; ml inclus ; 28 884 post-B4.3, **+450 en C1** = nouveau `backtest_metrics.py` 426 + `models/trades.py` +24, **+159 en C2** = nouveau `replay_contract.py` 67 + `grok_grid_atr_adaptive_v4.py` +92), scripts 16 490 lignes (P7 inclus ; 14 798 post-B4.3, **+843 en C1** : rapport P7 +336, moteurs +151, runners de campagne +225, dashboard +46, pipeline P6 +84, benchmarks +1 ; **+849 en C2** : moteurs +741, runners de campagne +90, gardes de contrat des rapports +18 ; `scripts/audit/` hors comptage).

## Flux runtime en 5 lignes

`__main__.py` → `main.KrakenBot` (setup:156, start:480, run:914) instancie settings, DB, EventBus, REST/WS via `connectors/exchange.py`
(dispatch sur `settings.exchange_name`, **obligatoire** depuis B1 : `bybit` / `binance` / Kraken ; WS Bybit en B2), `MultiStrategyRouter` (seule stratégie top-level
depuis B0.5), `ExecutionEngine`, `OrderManager`, `GlobalRiskManager`, `MultiPairAnalyzerRegistry`.
WS publie `MARKET_OHLC` → router `_handle_ohlc:275` → stratégie interne `on_ohlc`/`generate_signal` → `GeminiGlobalRiskManager.process_signal:321`
→ `TRADE_SIGNAL` → `ExecutionEngine._handle_signal:155` → REST (`place_limit_order`/`place_market_order`) → `TRADE_ORDER_FILLED`
→ router `_handle_trade_filled:612` → stratégie source via `bot_id`. Le collector (`collector.py`) est le même WS sans trading.

## config/

| Module | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `settings.py` | 979 | Pydantic settings (env + `strategies.yaml`) | `KrakenSettings:43`, `BinanceSettings:84`, `BybitSettings:126` (`credentials:229` — `readonly` = `BYBIT_*`, `trade` = `BYBIT_TRADE_*` ; `ws_url:66`, `ws_ping_interval_seconds`, `ws_pong_timeout_seconds`, `ws_watchdog_warn/zombie_seconds`, `ws_watchdog_resubscribe_alert_count`, `_validate_watchdog_thresholds:214`), `DatabaseSettings:252`, `RiskManagementSettings:279`, `TradingSettings:315`, `ScheduledTasksSettings:455`, `MultiStrategySettings:525` (`enabled` default False, forcé True par `strategies.yaml`), `FEE_MODEL_NAMES:580`, `ExchangeFees:583` (`kraken_defaults:600`, `binance_defaults:605`, `bybit_defaults:611`, **`from_name:621` = registre `--fees` (B4.2)**), `OrderSettings:675`, `_validate_router_runtime_alignment:734`, `Settings:783` (`exchange_fees:835` = fees live/paper des connecteurs, jamais lu par le backtest), `get_settings:951`, `reload_settings:969` | **`exchange_name` obligatoire** (`Literal[kraken\|binance\|bybit]`, sans default — dette B1 réglée) ; `validate_all` exige `BYBIT_TRADE_*` en live Bybit ; `trading.pair` default `"XBT/USDC"` (:324) ; `ScheduledTasksSettings.pairs` default `["BTC/USDC","ETH/USDC","SOL/USDC"]` (B2), `intervals` default 7 TF ; importe `strategies.multi_strategy_router` (couplage config→stratégies) ; `ScheduledTasksSettings` : cron défaut `30 3 * * *`, `backfill_days` = fenêtre de scan des gaps (défaut 3) (B3) |

## core/

| Module | Lignes | Rôle | Symboles clés | Dépendances |
|---|---|---|---|---|
| `event_bus.py` | 514 | Pub/sub async central | `EventType:55` (+ `SCHEDULER_TASK_SUCCESS/FAILED`, B3), `Event:83`, `EventBus:109` (`subscribe:155`, `publish:236`, `publish_and_wait:321`, `get_history:444`), `get_event_bus:489` | appelé par tout le runtime |
| `database.py` | 373 | Engine SQLAlchemy async + TimescaleDB | `Base:34`, `DatabaseManager:44` (`init_db:98`, `session:179`, `read_session:204`, `create_hypertable:231`), `get_db_manager:298`, `init_db:310`, `get_session:334` | `config.settings` |
| `exceptions.py` | 516 | Hiérarchie d'erreurs | `KrakenBotError:33`, `KrakenAPIError:78`, `RateLimitError:113`, `WebSocketError:147`, `InsufficientBalanceError:255`, `RiskLimitExceededError:291`, `OrderExecutionError:327`, `OrderCancelError:368` | noms « Kraken » réutilisés par Binance |
| `logger.py` | 297 | structlog + masquage secrets | `mask_secrets:50`, `configure_logging:156`, `get_logger:237`, `bind_context:254` | `config.settings` |

## models/

| Module | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `base.py` | 172 | Enums partagés | `TradeSide:55`, `TradeStatus:67`, `BotStatus:87`, `SignalType:105`, `PositionStatus:119`, `OrderType:133`, `OrderStatus:145`, `TradingMode:163` | `TradingMode` dupliqué dans `config/settings.py:26` |
| `market_data.py` | 349 | Hypertables OHLC/ticks + provenance des rows dérivées | `OHLCData:22`, `OHLCDerived:171`, `TickData:263` | colonne `exchange` default/server_default `"kraken"` (:69-70) ; PK (timestamp, pair, interval, exchange) ; `timestamp` = **fin de période** (`open_time + interval`, commentaire B4.1) ; `OHLCDerived` = table `ohlc_derived` (même PK, FK logique sans contrainte — la cible est une hypertable ; `method`, `source_stamps` JSONB, `source_sha256`, `vwap_policy`, `git_sha`, `note`) : **une row `market_data_ohlc` qui y figure n'est pas une donnée d'exchange** (24 rows 1 w USDT au 2026-09-24, migration `c3bd1e7a0001`) |
| `trades.py` | 843 | Tables trading | `Trade:31`, `BotState:208`, `OpenPosition:387`, `BacktestRun:567` (C1 : `sharpe_ratio:717`, `sortino_ratio:722`, `profit_factor:727` passés **nullable**, + `gross_profit_net:732`, `gross_loss_net:737`, `pf_excluded_trades:742`, `metrics_version:746`), `PaperBalance:788`, `utc_now:22` | C1 (migration `c1ae7a1c0001`, **appliquée en local et sur le serveur** ; vérifiée read-only le 2026-09-20 : `SELECT version_num FROM alembic_version` → `c1ae7a1c0001`) : un ratio indéfini est stocké **NULL, jamais 0** ; `max_drawdown_pct` = MaxDD sur la NAV journalière au pic courant, `profit_factor` net des deux jambes (NULL quand les pertes sont nulles — lire `gross_profit_net` / `gross_loss_net`, incomplet si `pf_excluded_trades > 0`) ; `metrics_version` NULL = row pré-C1. **Aucune colonne `replay_version`** (pas de migration en C2) : une row post-C2 y est indiscernable d'une row pré-C2 (reste de la dette 16) |
| `orders.py` | 216 | Ordres LIMIT suivis | `Order:26` | |
| `scheduled_tasks.py` | 107 | Log du scheduler | `TaskExecutionLog:17` | |

## connectors/

| Module | Lignes | Rôle | Symboles clés | Dépendances / attention |
|---|---|---|---|---|
| `exchange.py` | 218 | Protocol + factories | `ExchangeRestClient:26` (Protocol), `build_exchange_rest_client:145` (kwarg `read_only` → Bybit `key_role="readonly"`), `build_exchange_ws_client:183` | dispatch sur `settings.exchange_name` (`binance`, `bybit`:144, sinon Kraken) ; WS `bybit` → `BybitWebSocketClient` (:168, B2) ; appelé par `main.py`, `collector.py`, `order_manager.py` ; le Protocol ne reflète pas les signatures réelles (`signal_price` vs `reference_price`) — dette n°7 ; docstring `fetch_ohlcv:95` = contrat de timestamp (end-stamped **Bybit seulement**, B3) |
| `base_ws.py` | 107 | ABC WebSocket | `BaseWebSocketClient:25` (`connect:90`, `subscribe_ohlc:98`, `subscribe_ticker:102`) | |
| `binance/rest.py` | 1486 | REST Binance via ccxt + paper | `MIN_ORDER_SIZE:58`, `MIN_NOTIONAL:65`, `BinanceRestClient:68` (`get_balance:182`, `fetch_ohlcv:269`, `place_market_order:521`, `place_limit_order:824`, `get_order_status:1096`, `cancel_order:1166`, `initialize_paper_balance:1265`) | LOT_SIZE hardcodés BTC/ETH/SOL ; `place_margin_order:1237` = stub |
| `binance/ws.py` | 844 | WS Binance (kline/ticker) | `INTERVAL_MAP`/`BINANCE_INTERVAL_MAP:63`, `_pair_to_symbol:73`, `BinanceWebSocketClient:93` (`connect:148`, `subscribe_ohlc:217`, `_handle_kline:399`, `_data_flow_watchdog:560`, `_preventive_reconnect_timer:606`) | timestamp DB = `T + 1 ms` (:423) ; `exchange="binance"` hardcodé (:450) ; reconnect 23 h ; log `candle_timestamp` (B3) |
| `kraken/rest.py` | 1930 | REST Kraken (legacy, référence paper) | `normalize_asset_symbol:86`, `normalize_asset_balances:91`, `KrakenRestClient:100` (`place_market_order:327`, `place_margin_order:654`, `place_limit_order:1130`, `get_order_status:1433`, `fetch_ohlcv:1605`) | `normalize_asset_*` importés par `execution/order_manager.py:28` et `execution/risk.py:35` |
| `kraken/ws.py` | 1131 | WS Kraken v1 | `KrakenWebSocketClient:81` (`subscribe_ohlc:931`, `subscribe_trades:1011`, `ping:1124`) | map `BTC/USDC→XBT/USDC` (:74-75) ; `exchange="kraken"` (:645,669) |
| `bybit/rest.py` | 1628 | REST Bybit EU via ccxt + paper (B1) | `MIN_ORDER_SIZE:67`, `TICK_SIZE:73`, `MIN_NOTIONAL:82`, `BybitRestClient:105` (`load_markets:257`, `_round_amount:308`, `_round_price:313`, `_translate_error:341`, `get_balance:446`, `fetch_ohlcv:517`, `place_market_order:674`, `_live_market_order:828`, `place_limit_order:944`, `_rejected_post_only_order:1108`, `_live_limit_order:1154`, `get_order_status:1293`, `cancel_order:1342`) | `hostname="bybit.eu"`, `key_role` readonly/trade, `load_markets()` lazy, market BUY sans `price` (`quote_amount` → `cost`), PostOnly par défaut + rejet normalisé (`signal_metadata.reject_reason`), `fetch_order(params.acknowledged=True)`, mapping `retCode` ; validé en live le 9 sept (`skills/bybit.md`) ; **B3 : `fetch_ohlcv` via l'endpoint brut `publicGetV5MarketKline` — `timestamp = start + interval`, `vwap = turnover/volume`, candle en cours exclue** |
| `bybit/ws.py` | 1014 | WS Bybit EU public spot v5 (B2) | `INTERVAL_MAP`/`BYBIT_INTERVAL_MAP`, `MAX_ARGS_PER_REQUEST`, `_pair_to_symbol:97`, `_symbol_to_pair:102`, `BybitWebSocketClient:126` (`connect:202`, `subscribe_ohlc:264`, `_resubscribe_all:319`, `_handle_message:395`, `_handle_control_message:435`, `_handle_kline:460`, `_handle_ticker:546`, `_save_ohlc:581`, `_ping_loop:605`, `_data_flow_watchdog:662`, `_handle_stale_flow:731`, `_preventive_reconnect_timer:766`, `_reconnect:787`, `_cleanup:965`) | timestamp DB = `end + 1 ms` ; log `candle_timestamp` (B3) ; `exchange="bybit"` hardcodé ; `vwap = turnover/volume` ; ping applicatif 20 s / pong 10 s = zombie connexion ; watchdog flux agrégé 2 paliers (`BybitSettings.ws_watchdog_*`) + escalade Telegram ; subscribe par lots de 10 ; reconnect 23 h ; `MARKET_TICK` avec `price` (clé lue par les stratégies) et `bid`/`ask` None |

`kraken/futures.py` et `base_perps.py` (Kraken Futures) ont été supprimés en B0.5.

## execution/

| Module | Lignes | Rôle | Symboles clés | Dépendances / attention |
|---|---|---|---|---|
| `engine.py` | 934 | Consomme `TRADE_SIGNAL`, exécute, met à jour `BotState`/`OpenPosition` | `ExecutionEngine:43` (`start:120`, `_handle_signal:155`, `_execute_signal:220`, `_calculate_order_amount:409`, `_handle_buy_trade:554`, `_handle_limit_order_fill:678`, `_handle_sell_trade:706`, `execute_manual_order:851`) | type hint `rest_client: KrakenRestClient` (:82, TYPE_CHECKING) alors que Binance est injecté ; ne pas modifier sans review |
| `order_manager.py` | 869 | Cycle de vie LIMIT (polling, timeout, profit target) | `_parse_interval_minutes:64`, `OrderManager:84` (`load_pending_from_db:156`, `place_and_track:194`, `check_pending_orders:285`, `place_profit_target:526`, `on_ohlc:620`, `cancel_all_pending:842`) | importe `normalize_asset_*` de `kraken/rest.py` ; docstring « Kraken API status polling » |
| `risk.py` | 919 | Risk legacy + global multi-stratégie | `RiskCheckResult:49`, `RiskManager:99` (`check_order:181`), `GlobalRiskManager:539` (`register_strategy:596`, `check_order:622`, `check_emergency_stop_loss:826`, `get_risk_summary:903`) | idem import kraken ; ne pas bypasser |

## strategies/

| Module | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `base.py` | 513 | ABC + signal | `TradingSignal:40` (`to_dict:104`), `BaseStrategy:122` (`bot_id:171`, `effective_pair:180`, `on_ohlc:203`, `generate_signal:215`, `get_name:227`, `get_config:236`, `on_trade_filled:462`) | |
| `multi_strategy_router.py` | 714 | Dispatch par pair + overlay risque | `_INNER_STRATEGY_CLASSES:54` (8 classes), `_RiskOverlayEventBusProxy:66`, `MultiStrategyRouter:96` (`_init_inner_strategies:167`, `_handle_ohlc:275`, `_dispatch_to_strategy:376`, `_apply_risk_overlay:418`, `_handle_crash:467`, `_handle_trade_filled:612`) | registre statique : nouvelle stratégie = ajouter au dict :54 ; ne pas modifier sans review |
| `gemini_global_risk_manager.py` | 497 | 1 % rule, ATR SL, crash protector | `GeminiGlobalRiskManager:48` (`calculate_position_size:110`, `calculate_stop_loss:151`, `check_crash_protector:189`, `generate_crash_sells:252`, `process_signal:321`, `check_strategy_budget:443`) | `generate_crash_sells` pair default `"BTC/USDC"` (:256) |

Les 8 stratégies internes du router (fichier `grok_*`/`gemini_*`, toutes `pair`-aware, positions persistées dans `open_positions`) :

| Classe | Fichier (lignes) | TF signal | Params clés (`strategies.yaml`) | Statut |
|---|---|---|---|---|
| `GrokGridATRAdaptiveV4:91` | `grok_grid_atr_adaptive_v4.py` (811) | 4h (biais 1d/1w) | grid_levels 12, spacing 1.5-5 %, atr 14×4.0, recalc 6 h, order 10 USDC | actif BTC ; C2 : vente appariée au lot par `position_id` (`_match_sell_fill:704`, aucun repli de prix ; sans id = lot unique dont `sell_level == prix` en égalité Decimal), la tolérance `< 1` USD (double pop SOL, dette 14) a disparu ; le SELL apparié porte `amount_btc` et **pas** de `position_id` (`_emit_grid_signal:543`) ; fill non attribuable = journalisé, compté dans `fill_anomalies:182` (exporté par `get_config:786`), callback terminé sans retirer de lot ni créer de BUY de remplacement |
| `GrokSuperTrend4hRegime:58` | `grok_supertrend_4h.py` (555) | 4h (régime 1d) | st_atr 10, mult 3.0, sl_atr 3.5, alloc 10 % | actif BTC ; ETH/SOL off |
| `GrokDonchianChannelBreakoutV1:59` | `grok_donchian_breakout_4h.py` (515) | 4h (régime 1d) | upper 20, lower 10, sl_atr 3.5, adx 18, order 30 USDC | actif BTC |
| `GrokEMA27_125_ADX_ATR:63` | `grok_ema_adx_atr.py` (581) | 4h (régime 1d) | ema 20/50, adx 14, atr stop 3.5, trailing 3.0 | off (J30) |
| `GrokAdaptiveDCAWeekly:71` | `grok_adaptive_dca_weekly.py` (407) | 1d/1w | base 15 USDC, oversold ×2.5, rsi 30, alloc 25 % | off ; 0 trade ETH/SOL en backtest |
| `GeminiScalpingVolatilite:50` | `gemini_scalping_volatilite.py` (488) | 5m (filtre 1h) | rsi 7/30, sl 1.5 atr, tp 1.0 atr | KILL 1A |
| `GeminiSuiviTendanceMomentum:52` | `gemini_suivi_tendance_momentum.py` (478) | 4h (régime 1d) | adx 20, sl 2.5 atr, trailing 2.0 | KILL 1A (bug pullback `_prev_close_4h`) |
| `GeminiRetourMoyenne:50` | `gemini_retour_moyenne.py` (516) | 15m (filtre 1h) | dca 3×0.5 %, sl 2.0 atr | KILL 1A |

Statuts « actif » ci-dessus = configuration `strategies.yaml` ; en pratique rien ne tourne (services stoppés depuis le 7 sept 2026) et tout est revalidé en B4 avec les fees Bybit.

Supprimées en B0.5 (plus aucun fichier ni entrée `strategies.yaml`) : les 7 stratégies legacy top-level Kraken-era (`threshold_rolling`,
`adaptive`, `capitulation`, `bear_short`, `trend_following`, `grid_spot`, `grid_adaptive`) et les 3 stratégies hors registre
(`grok_ichimoku_cloud_4h`, `grok_vwap_trend_4h`, `grok_supertrend_short_4h`). `indicators/ichimoku.py` et `vwap.py` sont conservés
(analyzer + ML feature store).

## indicators/

| Module | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `multi_timeframe.py` | 1083 | Analyzer multi-TF par pair (lazy indicators) | `MarketRegime:76`, `MultiTimeframeAnalysis:95`, `MultiTimeframeAnalyzer:139` (`initialize:236`, `update:300`, `analyze:379`, `get_rsi:484`, `get_atr:510`, `get_adx:536`, `get_supertrend:584`, `get_ema:628`, `get_donchian:682`, `get_vwap:723`, `get_regime:748`, `get_features_dict:781`) | `pair="BTC/USDC"`, `exchange="binance"` en defaults (:239-240) ; indicateurs lazy à pré-enregistrer avant warmup ; `initialize:236` : `pair` et `exchange` **obligatoires** (B3, plus de default `binance`) |
| `multi_pair_registry.py` | 117 | Un analyzer par pair | `normalize_pair:34`, `MultiPairAnalyzerRegistry:43` (`get_or_create:60`, `get:75`, `update:79`, `initialize_all:91`) | `initialize_all` exchange default `"binance"` (:94) ; ne pas créer d'analyzer à la main ; `initialize_all:91` : `exchange` obligatoire (B3) |
| `atr.py` 115 · `adx.py` 229 · `ema.py` 105 · `rsi.py` 105 · `macd.py` 143 · `bollinger.py` 121 · `supertrend.py` 205 (dépend `atr`) · `donchian.py` 108 · `ichimoku.py` 136 · `vwap.py` 88 | | Indicateurs purs Decimal (`*Indicator` classes, ligne 10-31) | sans dépendance krakenbot | `Donchian` inclut la candle courante |

## data/ · scheduler/ · notifications/ · utils/ · racine

| Module | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `data/backfill.py` | 505 | Détection + comblement de gaps OHLC, exchange-agnostic (B3) | `Gap:98`, `GapResult:117`, `BackfillSummary`, `floor_to_grid:172` (1w ancré lundi), `is_grid_aligned:183`, `last_closed_timestamp:189`, `internal_gaps_from_rows:194`, `tail_gap:206`, `candle_to_row:231`, `fetch_timestamp_bounds:253`, `fetch_max_timestamps:275`, `detect_gaps:286`, `insert_candles:333`, `fill_gap:360`, `backfill_gaps:445` | convention DB = fin de période ; query LAG + `MAX(timestamp)` ; `ON CONFLICT DO NOTHING` batch 1000 ; **garanti pour Bybit seulement** (voir docstring) ; aucun littéral exchange |
| `scheduler/task_scheduler.py` | 250 | APScheduler : backfill de gaps périodique (B3) | `JOB_ID`, `TaskScheduler:43` (`start:85`, `stop:113`, `run_backfill:120`, `_write_logs`) | client REST **injecté** par le collector (read-only), job unique `gap_backfill` (`SCHEDULER_DAILY_BACKFILL_CRON`, défaut 03:30 UTC), `backfill_gaps` sur `settings.exchange_name` avec `lookback=backfill_days` ; 1 row `TaskExecutionLog` par gap + 1 row de run (`pair='*'`) ; events `SCHEDULER_TASK_*` ; aucun import de `scripts/` |
| `notifications/telegram.py` | 204 | Alertes Telegram | `get_notifier:27`, `TelegramNotifier:38` (`send_trade_fill:93`, `send_crash_protector:160`, `send_daily_summary:141`) | singleton module |
| `utils/time_utils.py` | 201 | Pagination ccxt | `minutes_to_ccxt_timeframe:10`, `calculate_pagination_steps:64`, `get_max_days_for_interval:134`, `ms_to_datetime:199` (B3) | |
| `main.py` | 1260 | Orchestrateur trader | `STRATEGY_REGISTRY:71` (router uniquement), `KrakenBot:76` (`setup:156`, `_setup_strategies:259`, `_collect_strategy_pairs:335`, `_register_strategy_budgets:362`, `_get_multi_strategy_ohlc_intervals:459`, `start:480`, `stop:585`, `_reconcile_positions_with_exchange:804`, `run:914`, `_periodic_order_check:977`, `_log_stats:999`), `main:1179` | mode legacy mono-stratégie supprimé : `multi_strategy.enabled=false` → `ValueError` dans `_setup_strategies` |
| `collector.py` | 417 | Collecteur WS + scheduler (service systemd) | `DataCollector:45` (`setup:85`, `start:154`, `run:262`), `main:357` | WS via factory ; TF depuis `settings.scheduler.intervals` (B2) ; REST client via factory **`read_only=True`**, prêté au `TaskScheduler` instancié pour tout exchange si `SCHEDULER_ENABLED` (B3, garde kraken-only supprimée) ; ferme le client au stop |
| `__main__.py` | 14 | `python -m krakenbot` → `main.main` | | |
| `backtest_metrics.py` | 426 | **C1** — système de mesure unique des deux moteurs et des benchmarks (`METRICS_VERSION:56` = 2, `ANNUALISATION_DAYS:57` = 365) | `EquityPoint:64`, `ExternalFlow:75`, `TradeLeg:83`, `DailySeries:95` (`defined_returns:105`, `to_dict:108` = `equity_daily` des résultats), `MetricsVersionError:119`, `daily_grid:128`, `resample_daily:144`, `sharpe_ratio:207`, `sortino_ratio:220`, `max_drawdown_pct:231`, `cagr_pct:245`, `calmar_ratio:252`, `ProfitFactorSummary:264` (`profit_factor:271`), `net_trade_pnls:277`, `profit_factor_from_sums:293`, `MetricsResult:314`, `compute_metrics:330`, `mean_available:376`, `entry_metrics_version:384`, `require_metrics_version:396`, `fmt:417` | module **pur** (Decimal in, float / None out) : aucune I/O, aucun import de `scripts/` (règle B3) ni de `krakenbot` — consommé par `scripts/backtest.py`, les runners P6/P7/walk-forward, `p7_report.py`, `filter_p6_survivors.py`, `generate_p6_report.py`, `compute_benchmarks.py` ; rééchantillonnage **journalier UTC** (ancre `(start, starting_balance)` prioritaire, forward-fill, flux externes bucketés sur `(t_{k-1}, t_k]`, index de performance `I_0 = 1` — un dépôt ne masque plus un drawdown) ; Sharpe / Sortino annualisés `√365` sur les rendements journaliers et **`None` quand indéfinis, jamais 0** ; `max_drawdown_pct_daily` = chiffre de sélection, `max_drawdown_pct_engine` = diagnostic seul (jamais un critère inter-familles ; aucun des deux ne capture les mèches) ; PF net des deux jambes (`pnl − buy_fee_alloc`), `None` quand les pertes sont nulles — l'ambiguïté ∞ / 0-0 se lève sur `gross_profit_net` / `gross_loss_net`, PF incomplet dès `pf_excluded_trades > 0` ; `require_metrics_version` refuse tout fichier mixte ou pré-C1 |
| `replay_contract.py` | 67 | **C2** — estampille de contrat de *ce que les moteurs simulent* (`REPLAY_VERSION:32` = 2) | `ReplayVersionError:35`, `entry_replay_version:39`, `require_replay_version:45` | pendant exact du garde C1 : `backtest_metrics` versionne **comment on mesure**, ce module **comment on rejoue** (`scripts/backtest.py`) — séries qui nourrissent les indicateurs, instants de décision et de fill, préenregistrement lazy et dimensionnement du warmup, appariement des ventes aux lots, comptage des rejets ; la clé vit au **top-level** d'une entrée de résultats, d'un dump `--trades-out`, d'une capture ou d'un sidecar d'equity — **jamais** dans `BacktestMetrics.to_dict()` (contrat gold-hashé de C1 intact) ; version `1` volontairement inutilisée pour qu'« absent » (tout fichier P6 / P7 / B4 / C1) et `1` ne se confondent jamais ; `require_replay_version` refuse les fichiers mixtes ou pré-C2 (entrées `"error"` sautées, comme le garde C1) ; module pur, aucune I/O, aucun import de `scripts/` ni de `krakenbot` ; **pas de colonne `replay_version` sur `backtest_runs`** (aucune migration en C2) → rows pré/post-C2 indiscernables, reste de la dette 16 |

## ml/ (désactivé, `ml.enabled: false`)

`config.py` 65 (`MLSettings:30`) · `db_models.py` 324 (`MLFeatureRow:21`, `MLExternalData:282`) · `features/feature_store.py` 664 (`FeatureStore:48`, exchange default `"kraken"` :472) ·
`features/external_data.py` 185 (`ExternalDataFetcher:28`) · `inference/enhancer.py` 95 (`MLSignalEnhancer:40`) · `models/lightgbm/` (`RegimePredictor:20`, `SignalFilter:25`, `VolForecaster:20`) · `monitoring/drift.py` 71.

## scripts/

| Script | Lignes | Rôle | Symboles clés | Attention |
|---|---|---|---|---|
| `backtest.py` | 4075 | Moteurs backtest signal + grid (contrats `METRICS_VERSION` 2 / `replay_version` 2) | `_backtest_chunk_days:46`, `_load_candles_chunked:59`, `_override_pair_in_params:102`, `PairCosts:120`, `load_pair_costs:131`, `resolve_fee_model:153`, **`capture_effective_params:183`** (params effectifs par stratégie, source `class_default`/`passed`/`override` — B4.3), **`RejectionLedger:237`** (C2 R3 : comptage par (ordre, cause) distincte — `MINIMAL_CAUSES:253` toujours présentes même à 0, `note:266` renvoie True au premier couple, `prune:276` oublie les ordres sortis du pending sans décrémenter les totaux, `add_counts:281` fusionne les `fill_anomalies` de la stratégie, `summary:287` = bloc `{unit, by_cause, events}`), **`IndicatorRequirement:313`** (C2 R2 : `candles:328` = bougies de readiness de la classe d'indicateur, `lazy:348` = créé au premier `get_*`, `preregister:358`), **`indicator_requirements:378`** (table des lectures analyzer des 8 stratégies, lue sur les **attributs de l'instance** et non sur `effective_params`), `preregister_indicators:452`, `warmup_needs:464`, **`load_context_series:472`** (fenêtre calendaire gardée en plancher, extension arrière **monotone** bornée à `required × interval × 3`, rapport `required`/`loaded`/`extended_by`/`stale_by_candles`/`largest_gap_candles`/`sufficient` — un trou est signalé, jamais comblé), `_load_candles_before:534`, **`ReplayEvent:565`** + `_GRID_REPLAY_PHASE:586` (C2 R1 : ordre à timestamp égal `exec` 0 → `1w` 1 → `1d` 2 → décision `4h` 3), `BacktestTrade:590` (champs d'audit `liquidity`/`fee_rate`/`fee_base_usdc`/`reference_price`/`spread_pct`/`slippage_pct`, `buy_fee_alloc`, `forced_liquidation`), `BacktestMetrics:617` (C1 : ratios `None` = indéfini jamais 0, `max_drawdown_pct_daily` vs `_engine`, `pf_excluded_trades`, `equity_daily` ; `net_pnl` = chaque fee comptée une fois — B4.3 ; `to_dict:673` = contrat `METRICS_VERSION` 2 qui alimente les gold hashes, `equity_daily_dict:708`, `profit_factor_display:713`), `_buy_fee_from_cost_basis:731`, `apply_shared_metrics:738` (délègue à `krakenbot.backtest_metrics`, appelé en dernier par les 2 moteurs, ne touche pas l'état de simulation), `BacktestEngine:787` (`__init__:794` — `_requirements`/`_warmup_needs`/`warmup`, `rejections:850`, `dca_counters:854` (C2 preuve 6, DCA seul), `_apply_params_override:886`, `_load_inner_strategy_params:911` — params résolus sous le nom d'entrée du router, défauts de classe pour les grok (dette 13), `_load_context_series:998`, `warmup_summary:1020`, `_build_replay_sequence:1027` — séries de contexte dimensionnées en bougies, bougie `start` nourrie une seule fois, `load_historical_data:1163`, `_resolve_fill:1205` — market au **open de N+1**, limit au touch, `_costs_for_pair:1234`, `execute_signal:1241` — `order_id` du ledger + plancher `min_order_usdc`, `calculate_final_metrics:1520`, `run:1625` — préenregistrement lazy aux params effectifs `:1820-1823`, `rejections_summary:1992`, `dca_counters_summary:1996`, `print_report:2004`, `save_to_database:2087`, `save_trades_to_database:2150`), `GridBacktester:2187` (`GRID_STRATEGIES:2194`, `_INVENTORY_DUST_BTC:2198`, `__init__:2200` — `decision_trace:2272`, `rejections:2277` + `_level_ordinals:2278` (identité d'un niveau = ordinal posé à sa première apparition), `_costs_for_pair:2318`, `_record_equity:2325`, `_load_grid_strategy_params:2377`, **`_build_grok_grid_replay_sequence:2400`** — C2 R1 : vraies séries 4 h / 1 d / 1 w chargées sur tout le run, décisions aux clôtures 4 h ≥ `start`, exécution à `candle_interval`, `ValueError` si `candle_interval >= 240`, `_create_grok_grid_strategy:2462` (préenregistrement `:2483-2485`), `_get_grok_grid_pending_orders:2497`, `_process_grok_grid_buy_fill:2543`, **`_process_grok_grid_sell_fill:2592`** — C2 R4 : lot cherché par `position_id` **avant** toute mutation (id inconnu ou inventaire insuffisant au-delà de la poussière → rejet compté, rien ne bouge), quantité ≠ lot ou lot encore ouvert après le callback → `RuntimeError`, `run:2865`, **`_replay_grok_grid:2957`** — `exec` remplit (buys puis sells), met à jour l'analyzer, `on_ohlc`, marque l'equity ; `ctx` = analyzer seul ; `decision` = `decision_trace:3000` (ATR 4 h, régimes 1 d / 1 w) puis `_handle_ohlc`, `_load_context_series:3056`, `warmup_summary:3078`, `_liquidate_lot:3085`, **`_force_close_open_positions:3149`** — liquidation terminale MARKET au dernier close tradeable, taker + spread + slippage, idempotente, `liquidation_summary:3269` — bloc `liquidation` (résidu, `inventory_divergence_btc`, `net_pnl_lot_basis`), `rejections_summary:3304` + `_merge_strategy_anomalies:3314`, `_calculate_final_metrics:3320` — `net_pnl` = cash réalisé `ending − capital`, `print_report:3408`, `save_to_database:3494`), `build_parser:3536` (`--equity-out` C1), **`parse_args:3658`** (gardes C2, exit 2 : `--cross-validate` refusé sur une stratégie grid — la branche construit un `BacktestEngine` —, `--interval` < 240 exigé sur le grid), `_print_replay_blocks:3693` (section console warmup / rejets / compteurs DCA, appelée par les 2 `print_report`), `_db_ratio:3713`, `dump_equity_jsonl:3720`, `dump_trades_json:3756` (+ `metrics_version`, `replay_version`, `equity_daily`, `warmup`, `rejections`, `dca_counters`, `liquidation`), `main:3866` | **`--fees {bybit,binance,kraken}` obligatoire (B4.2)**, `fee_model` keyword-only requis dans les 2 constructeurs (`resolve_fee_model`), indépendant de `exchange` = source de données (toujours passer `--exchange binance`) ; maker sur les fills limit/grille, taker + spread + slippage sur les fills market et à la liquidation terminale du grid (trades `forced_liquidation`) ; `--trades-out`, `--equity-out` (C1, run unique), `--pair-costs-file`, `--min-order-usdc` (défaut 1 = comportement historique) ; `.env` chargé dans `main()` seulement. **C2** : le grid grok décide sur les vraies clôtures 4 h et exécute sur les bougies de trading (avant C2 les bougies 5 m étaient étiquetées 240 → indicateurs « 4 h » nourris de barres 5 m) ; tout indicateur lazy est préenregistré aux **paramètres effectifs** avant le warmup (avant C2, liste codée en dur → SuperTrend (8, 2.0), Donchian (15, 10), EMA 200 1 d du DCA muettes après le warmup) ; le warmup est dimensionné en bougies et son bloc `warmup` dit `sufficient` ou non ; les rejets sont comptés par (ordre, cause) dans les deux moteurs ; ventes grid appariées par `position_id` (dette 14 **résolue**, `inventory_divergence_btc` retombé à la poussière Decimal sur SOL) ; provenance `replay_version` 2 — les fichiers pré-C2 sont refusés par les runners, mais **la table `backtest_runs` n'a pas de colonne `replay_version`** (pas de migration en C2 : une row `--save` post-C2 y est indiscernable d'une row pré-C2, dette 16 « reste »). **Dette 13 élargie** : params grok résolus sur les défauts de classe, et un SELL limit rempli à un prix meilleur que sa limite reste `unmatched_sell_fills` (réconciliation signalée, jamais attribuée) ; **dette 17** : cost basis au **dernier** prix d'entrée en accumulation (moteur signal), fills grid déclenchés au touch du low/high de la bougie d'exécution sans file d'attente ni fill partiel, equity valorisée au close ; **dette 18** : `analyzer.update()` de la boucle **plus** le `on_ohlc` des 3 stratégies gemini → double alimentation, période effective des indicateurs ~divisée par deux en replay (bloque P12) |
| `run_p6_backtests.py` | 875 | 24 backtests P6 en multiprocessing + reprise | `Job:86` (champs `fees`, `pair_costs_file`, `min_order_usdc`), `build_job_list:131`, `load_existing_results:175`, `FeeModelMismatchError:187`, `CampaignConfigMismatchError:206`, `MetricsVersionMismatchError:210` (C1), `ReplayVersionMismatchError:223` (C2), `_campaign_signature:236`, `_check_entry:250` (ordre fees → coûts de campagne → métriques → replay), `assert_homogeneous:275`, `filter_pending_jobs:291`, `save_result_atomic:339`, `StatusState:376`, `run_single_backtest_job:430`, `_async_run_cross_validated:456` (`_run_segment` interne : train / test / all), `detect_n_workers:565`, `_apply_result:595`, `run_serial:635`, `run_parallel:650`, `parse_args:730`, `main:791` | `--fees` obligatoire (B4.2), `--pair-costs-file` / `--min-order-usdc` (B4.3) enregistrés dans chaque résultat avec `effective_params` et le bloc grid `liquidation` par segment ; chaque entrée (y compris une entrée en erreur) porte désormais `metrics_version` = 2 (C1) et `replay_version` = 2 (C2) **au niveau top, jamais dans `to_dict()` des métriques**, et chaque segment ajoute `equity_daily` (NAV quotidienne, C1) plus `rejections` / `warmup` / `dca_counters` (C2, collectés par `hasattr` : absents des moteurs qui ne les exposent pas, `dca_counters` seulement hors `None`) ; la reprise refuse un fichier d'un autre modèle de fees, d'une autre signature de campagne, d'un autre contrat de métriques (ou pré-C1) ou d'un autre contrat de replay (ou pré-C2) → exit 2 ; `assert_homogeneous` contrôle **tout** le fichier cible même quand aucune clé ne collide (un job de clé neuve ne peut plus être appendu à un JSON B4 historique) et `--force` ne recalcule qu'un fichier déjà homogène, donc n'écrase plus jamais un legacy ; statut `logs/p6_status.json` ; sorties B4 : `results/B4_P6_*` |
| `run_p6_walkforward.py` | 374 | Walk-forward fenêtres (12 m train / 3 m test, seule la fenêtre test est rejouée) | `generate_windows:51`, `run_single_backtest:82` (rend metrics, `liquidation`, `equity_daily` — C1 —, bloc replay — C2), `aggregate_windows:120` (moyennes None-aware + `mean_metrics_n`, C1), `parse_args:145`, `main:180` | `--fees` obligatoire (B4.2), `--pair-costs-file` / `--min-order-usdc` (B4.3) ; chaque survivant est vérifié entrée par entrée avant tout run — `fees`, coûts de campagne, `metrics_version` (C1), `replay_version` (C2) — et tout écart, y compris un fichier pré-C1 / pré-C2, sort en exit 2 avant ouverture de la DB ; les résultats réécrivent ces quatre marqueurs et chaque fenêtre porte `liquidation`, `equity_daily`, `rejections`, `warmup`, `dca_counters` ; `aggregate_windows` ne force plus un `None` à 0 : moyenne sur les seules fenêtres qui définissent la métrique, effectif dans `mean_metrics_n`, `consistency_score` = fenêtres à Sharpe test défini **et** > 0 ; `--survivors` / `--output` ; fichier survivants `{}` (sélection B4.3 vide) → sortie 0 sans écriture |
| `filter_p6_survivors.py` 335 · `generate_p6_report.py` 433 · `p6_data_coverage.py` 202 · `p6_5_diagnose_dca.py` 287 · `p6_5_diagnose_filters.py` 300 · `compute_benchmarks.py` 340 · **`b4_flags.py` 126** | | Pipeline P6 (critères, rapport, couverture, diagnostics, buy&hold/DCA) · règle de flag B4.3 · **gardes de contrat C1 (`metrics_version`) et C2 (`replay_version`)** | `metric:58` (absent/null = indéfini, jamais coercé en 0), `max_dd:69` (`max_drawdown_pct_daily`, clé pré-C1 en repli), `check_criteria:75`, `check_consistency:109`, `check_beats_benchmark:124`, `parse_args:139`, `main:150` · `_m:37`, `_dd:48`, `load_json:63`, `main:69` · `compute_risk_metrics:69` (délègue à `krakenbot.backtest_metrics.compute_metrics`), `_anchor_time:94`, `load_daily_candles:99`, `_costs:115`, `buy_and_hold:122`, `dca_fixed_weekly:163`, `parse_args:232`, `run:262`, `main:332` · `flag_segment:31`, `flag_entry:55`, `flag_walkforward_entry:75`, `collect_flags:94`, `render_flags_markdown:106` | `filter_p6_survivors.py` / `generate_p6_report.py` / `b4_flags.py` sans dépendance runtime (JSON in/out) ; `compute_benchmarks.py` et les trois `p6_*` lisent la DB ; chemins d'entrée/sortie en arguments (défauts legacy) ; **C1** : les deux consommateurs du fichier phase D exigent `metrics_version == 2` (`require_metrics_version`, `filter:164` / `generate:111`) **et** un fichier de benchmarks de la même version (`filter:177` / `generate:101`) → sinon message explicite et **exit 2** ; une métrique indéfinie s'affiche `n/a` et **échoue** son critère (`check_criteria:75`), un Sharpe ou une jambe de benchmark indéfinie ne peut pas être battue (`check_beats_benchmark:124`) ; **C2** : `require_replay_version` sur le même fichier phase D (`filter:169` / `generate:116`, mixte ou pré-C2 → exit 2) — le fichier de benchmarks ne porte pas de `replay_version` (aucune stratégie n'y est rejouée) ; `compute_benchmarks.py --fees {bybit,binance,kraken,none}` (obligatoire) `--pair-costs-file` : B&H = achat market taker + spread + slippage par paire, DCA fixe = limite maker, et **le DCA tient désormais un compte cash dont chaque dépôt hebdomadaire est un `ExternalFlow`** → Sharpe redevenu comparable, MaxDD qui n'est plus l'artefact 100 % (défaut D6 de l'audit red-team B4) ; sortie estampillée `metrics_version: 2` ; `generate_p6_report.py` sections « Flagged runs » et « Effective parameters » ; `b4_flags.py`, `p6_data_coverage.py` et les deux `p6_5_diagnose_*` inchangés depuis B4.3 |
| `run_p7_grid_search.py` | 1387 | Grid search P7 (phase 1 cross-validate, phase 2 walk-forward, report) | `P7Job:113` (champs `fees`, `pair_costs_file`, `min_order_usdc`), `build_phase1_jobs:173`, `WalkForwardWindow:225`, `select_top_k_per_combo:285`, `_safe_sharpe:315` (Sharpe absent / NaN = −inf, classé dernier), `build_phase2_jobs:329` (refus au seed : fees, `metrics_version`, coûts, `replay_version`), `FeeModelMismatchError:399`, `MetricsVersionMismatchError:418` (C1), `ReplayVersionMismatchError:431` (C2), `CampaignConfigMismatchError:456`, `_check_entry:474`, `filter_pending_jobs:499`, `_assert_results_fee_model:553`, `StatusState:612`, `_async_run_job:682` (`_run_segment` : train / test / all en phase 1), `_apply_result:821`, `run_parallel:885`, `parse_args:990`, `_assert_benchmarks_version:1109` (C1), `load_benchmark_sharpe:1121`, `load_benchmark_details:1137`, `_run_report_phase:1163`, `main:1250` | sorties B4 `results/B4_P7_phase1_cross_validate.json` / `B4_P7_phase2_walk_forward.json` / `B4_P7_optimization_report.md` / `B4_P7_final_selection.json` (`--output`, `--phase1-input`, `--phase2-input`, `--benchmarks`, `--report`, `--selection` ; les défauts pointent sur les fichiers P6/P7 historiques, sans `fees` ni `metrics_version` ni `replay_version` → exit 2), statut `logs/p7_status.json` ; `--fees` requis pour toutes les phases, signature de campagne (`pair_costs_file` comparé comme chaîne, `min_order_usdc`) validée contre les fichiers d'entrée en phase 2 et report ; chaque entrée porte `metrics_version` = 2 (C1) et `replay_version` = 2 (C2) au niveau top et chaque segment ajoute `equity_daily` (C1) plus `rejections` / `warmup` / `dca_counters` (C2) ; la reprise (`_check_entry` : fees → coûts → métriques → replay) comme le seed de la phase 2 refusent tout fichier mixte ou pré-C1 / pré-C2, sans échappatoire `--force` ; **phase report : `--benchmarks` obligatoire depuis C1** — le fichier de `compute_benchmarks.py` doit porter le même `metrics_version` (`_assert_benchmarks_version`), sinon exit 2 ; les constantes v1 de `p7_report.BENCHMARK_SHARPE` ne s'appliquent plus à des résultats v2, et `load_benchmark_details` lit `max_drawdown_pct_daily` (pré-C1 : `max_drawdown_pct`), valeurs indéfinies laissées à `None` ; **`LOG_LEVEL` non honoré** (aucun script n'appelle `configure_logging`) → filtre de log côté serveur (`skills/backtest.md`) |
| `p7_grids.py` | 123 | Grilles P7 (4 stratégies, noms réels des kwargs ; spacing `[0.015, 0.020, 0.025, 0.030]` — plancher Bybit 2 %) | `expand_grid:93`, `build_phase1_combos:107` (212 jobs), `total_phase1_jobs:121` | inchangé depuis B4.3 ; consommé par `run_p7_grid_search.py` (phase 1) |
| `p7_report.py` | 1184 | Agrégation walk-forward P7 + 7 critères + inéligibilité + markdown ; **deux chemins** : contrat C1 (`metrics_version` 2, None-aware, PF sommé) et chemin legacy v1 reproduit bit à bit pour les fichiers B4 | `BENCHMARK_SHARPE:80` (constantes v1 P6 Binance, contaminées D6), `LOW_FREQUENCY_STRATEGIES:87`, `WalkForwardAggregate:104` (`profit_factor_oos:136` = Σ `gross_profit_net` / Σ `gross_loss_net` ; `n_sharpe_train/oos`, `n_pf_oos`, `pf_excluded_trades_oos`, `legacy`), `_safe_float:165` (v1 seul), `_has_sums:196`, `_pf_fields:210`, `aggregate_walk_forward:259`, `CriterionResult:391`, `ConfigVerdict:400`, **`apply_selection_criteria:448`** (v2) / **`_apply_selection_criteria_v1:574`** (pré-C1, verbatim), **`ineligible_configs:667`**, `_min_trades_per_window:685`, `build_selection:691` (`ineligible=` → `ineligible_flagged`, jamais dans `selected_for_paper` — règle GO P7 n° 1), `generate_report:839`, `DCA_SHARPE_NOTE:911` / `DCA_SHARPE_NOTE_V2:924`, `_render_markdown:937`, `_group_phase1_by_combo:1167` | **un rapport = un contrat** : `MetricsVersionError` si les entrées phase 1 / phase 2 ne partagent pas la même `metrics_version` (:867-877) ; en v2 `--benchmarks` devient **obligatoire** (:885 — les constantes `BENCHMARK_SHARPE:80` ne s'appliquent qu'aux fichiers pré-C1) et **C2** `require_replay_version` (:882) refuse un fichier pré-C2 ou mixte, le chemin legacy v1 (fichiers B4 gelés, aucune clé de version) restant délibérément intact ; v2 : moyennes prises sur les fenêtres qui définissent la métrique, critère 2 sur les gains/pertes **sommés** (`inf` sans aucune perte → passe, `0/0` → indéfini → échoue), métrique indéfinie = échec annoté ; v1 : `_safe_float` coerce à 0 et moyenne les PF par fenêtre ; note de lecture choisie par `legacy` (`DCA_SHARPE_NOTE:911` v1 / `DCA_SHARPE_NOTE_V2:924` v2) ; benchmarks Sharpe seul pour le critère 7, return/MaxDD pour la table (règle GO P7 n° 2) ; rapport `results/B4_P7_optimization_report.md` + `B4_P7_final_selection.json` du 2026-09-15 = chemin **legacy v1** (0 sélection, 5 inéligibles) ; > 1 000 lignes depuis C1/C2 |
| `binance_vision_import.py` | 346 | Import CSV Binance Vision → `market_data_ohlc` | `pair_to_binance_symbol:71`, `parse_klines_csv:109`, `import_month:164`, `main:231` | batch 1000 rows ; ms/µs auto ; **timestamp = `open_time + interval` (fin de période) depuis B4.1** ; `.env` chargé dans `main()` (B4.2) |
| `bybit_kline_import.py` | 346 | Import historique Bybit EU (B3, one-shot) | `ImportStats:74`, `resolve_since:118`, `import_pair_interval:163`, `format_table:258`, `main:341` | REST brut v5 via `fetch_ohlcv` (`vwap`, end-stamped), reprise `MAX(timestamp)` sauf trou de tête (`MIN` trop récent → scan complet), `--force-full`, `--dry-run`, batch 1000 `ON CONFLICT DO NOTHING`, condition d'arrêt loggée (`last_responses`) ; garde `EXCHANGE_NAME=bybit` |
| `backfill_gap.py` | 148 | Backfill de gaps, exchange courant (B3) | `parse_args:49`, `format_summary:76`, `main:143` | wrapper mince de `krakenbot.data.backfill` ; `--dry-run`, `--lookback-days`, `--pairs`, `--intervals` ; client read-only via factory ; warning si exchange ≠ bybit |
| `download_multipair_ohlc.py` | 378 | Download ccxt Binance multi-pair | `download_pair_interval_sync:160` | `exchange="binance"` defaults (:94,112) ; ccxt sync |
| `import_external_ohlc.py` | 403 | Fusion candles Binance sans écraser | `fetch_candles_from_binance:66`, `save_candles_preserve_existing:156` | |
| `dashboard.py` | 3730 | Dash UI (trades, positions, backtests) ; affichage C1-aware des métriques de backtest | `_read_sql:171`, `fetch_ohlc_data:411`, `fetch_stats:497`, `fetch_current_price:654`, **`_is_missing:673`, `_ratio_display:677`, `_c1_label:684`, `_profit_factor_display:691`** (C1), `fetch_backtest_runs:713` (lit `gross_profit_net`, `gross_loss_net`, `pf_excluded_trades`, `metrics_version`), `run_backtest_in_thread:924`, `create_candlestick_chart:1049`, callbacks `update_*:1958-3436` (dont `update_backtest_details:2942`), `create_comparison_metrics_table:3568` | `"XBT/USDC"` sur 11 lignes (`fetch_ohlc_data:411`, `fetch_current_price:654`, SQL `:743` / `:789`, dropdown `:1733-1737`) ; SQL brut via `_read_sql:171` ; dropdown backtest construit depuis `strategies.yaml` ; `DASHBOARD_FEE_MODEL:60` = `"bybit"` passé au `BacktestEngine` (B4.2) — la source de données reste le default `kraken` et les stratégies grid sont routées vers le moteur signal (annexes B4.2) ; **C1** : un ratio NULL s'affiche `n/a` et jamais 0 (`_ratio_display:677`), le profit factor est dérivé des sommes stockées (`∞` si gains sans perte, `n/a` en 0/0 ou ligne pré-C1, mention « incomplete: n lot(s) with unknown cost » si `pf_excluded_trades > 0`, `_profit_factor_display:691`, utilisé en `:2972` et `:3603`), et les suffixes « (net) » / « (daily) » ne sont posés que si `metrics_version >= 2`, sinon « (v1) » (`_c1_label:684`, `:3064` / `:3080`) — dépend de la migration `c1ae7a1c0001` (4 colonnes nullable sur `backtest_runs`) ; aucune garde `replay_version` ici : le dashboard lit la DB, pas les fichiers de campagne |
| `status.py` | 232 | État bot/DB en CLI | `get_status:59` | |
| `build_ml_features.py` 196 · `fetch_external_data.py` 82 | | Pipeline ML (off) | | |
| `seed_test_data.py` 143 · `test_all.py` 233 · `test_binance_connection.py` 105 · `test_binance_ws.py` 106 | | Smoke tests manuels | | `seed_test_data` insère `exchange="kraken"` (:74) |
| `audit/bybit_common.py` · `audit/bybit_q1_endpoints.py` … `bybit_q8_price_diff.py` · `audit/bybit_key_diag.py` · `audit/bybit_b1_roundtrip.py` | | Audit B0/B1 Bybit EU (hors comptage) | | `bybit_b1_roundtrip.py --trade` place 3 LIMIT réels annulés/rejetés (jamais MARKET) ; `bybit_key_diag.py` : clés + wallet UNIFIED/FUND (read-only) |
| `audit/b4_stamp_lib.py` · `audit/b4_timestamp_audit.py` · `audit/b4_restamp_binance.py` | | B4.1 : helpers purs (votes, frontières, manifeste, fenêtres, ledger, règles A/B), audit read-only (modes pré / `--post-migration` / `--pre-migration-view`), migration re-stamp (`--dry-run --explain` / `--execute`, table de progression transactionnelle) — hors comptage | | tests `tests/test_scripts/test_b4_*.py` (48) |
| `audit/b4_2_reference_capture.py` | | B4.2 : harnais de régression iso-fees — `capture` (run → JSON schéma 1, Decimal exact), `compare` (projection byte-exacte), `verify-fees --fees` (maker/taker/spread/slippage trade par trade), `normalise-log` — hors comptage | | tests `tests/test_scripts/test_b4_2_reference_capture.py` ; captures et rejeux dans `results/b4_2_*` |
| `audit/c3_common.py` · `audit/c3_anchor.py` · `audit/c3_entry.py` · `audit/c3_benchmark.py` · `audit/c3_select.py` · `audit/c3_continuity.py` · `audit/c3_verdict.py` | | **C3 — outillage du protocole `docs/protocole_c3.md` v2.1 (sha256 `9300f4e5…`, amendé le 2026-09-23 ; C3a : `d931293`), ordre § L.1 (hors comptage, 7 045 lignes)** : `c3_common` = constantes gelées (`THRESHOLDS`, 23 seuils ; `COMBINATIONS` hors registre), vocabulaire des raisons / statuts / états, noyau numérique et rejeu du tirage § F.2 (`cagr_rows`, `replay_bootstrap`), admission de l'évaluation § L.1 (`evaluation_admission`), accesseur strict, `canon` / `sig` ; `c3_anchor` = ancrage § A.3, estampilles § A.4, registre de variantes § A.6 ; `c3_entry` = validité d'entrée § I-A, refus § D.3 — écrit toujours ; `c3_benchmark` = comparateur du préfixe § C.3, comparabilité § C.5, `λ` § C.4 / § F.2 g ; `c3_select` = projection § A.7, admissibilité D1-D6, plancher P1-P3, classement § A.9, abstention § A.11 ; `c3_continuity` = admission en tête, contrat § B, cinq clauses en états (listes closes § B.8) ; `c3_verdict` = contrat d'instrument en tête, rejeu § F.2 d, issue § H, chaîne § L.2, `verify_chain` (17 contrôles), orchestration `chain` | | pur, lecture seule, aucun accès base, `--now` injectable ; codes § I.1 (0 · 1 diagnostic `invalide: true` · 2 rien publié sauf `c3_entry`) ; admission § L.1 (évaluation réelle avec ses trois porteurs) ; tests `tests/test_scripts/test_c3_*.py` (8 fichiers, **932**) ; artefacts `results/c3a_entry_validation/` (historique v2.0) ; règles et sections d'origine v2.1 dans `skills/backtest.md` § « Validation C3 » |
| `audit/data_inventory.py` | | **Inventaire de données — lecture seule (2026-09-22, hors comptage)** : couverture `exchange='binance'` par (paire, TF) — premier / dernier stamp, count, attendu sur la grille, trous `LAG` (bougies manquantes, durée, sur grille) en vue complète puis zoom 2022-01 → 2023-06 ; le trou USDC par TF et « même événement » ; Kraken / Bybit sur `(2022-01-01, 2024-01-01]` ; `HEAD` Binance Vision 1 d (six symboles × 10 mois) ; arithmétique d'amorçage (règle C2 `_WARMUP_GAP_TOLERANCE = 1`, `required` ∈ {14, 50, 200}) ; **`--pairs` (2026-09-23)** = filtre par paires sur tous les exchanges, sans changer la mesure, inscrit dans l'artefact (clé `pairs`) et l'en-tête Markdown, absent = artefact inchangé octet à octet | `step_of:176`, `is_on_grid:181`, `holes_from_stamps:187`, `describe_hole:194`, `is_blocking:218`, `grid_count_closed:235`, `series_summary:259`, `admissible_starts:296`, `usdc_hole:344`, `same_event:355`, `filter_series:372`, `build_artifact:573`, `reconcile_rejeu:636`, `render_markdown:923`, `vision_head:978`, `SUMMARY_SQL:1003`, `HOLES_SQL:1017`, `collect:1033`, `parse_args:1094`, `main:1165` | pur hors `collect` (session `transaction_read_only = on`, `statement_timeout` 300 s, agrégats et trous seuls traversent le tunnel) ; `--now` = `generated_at` **et** borne `timestamp <= now` (deux passages identiques octet à octet) ; réutilise `c3_common` par import (`WEEK_MINUTES`, `expected_candles`, `longest_missing_run` — épinglés par identité dans les tests) ; codes 0 / 2 (2 aussi pour une paire `--pairs` sans série : rien publié) ; `--skip-vision`, `--reconcile-rejeu` ; tests `tests/test_scripts/test_data_inventory.py` (39) ; artefacts `results/data_inventory_20260923/` (22/09) et `results/data_inventory_usdt_20260923/` (post-import USDT, outil `8fdaa2a` sans `--pairs`) |
| `audit/warmup_at.py` | | **Mesure SOL/D2 — amorçage C2 du moteur grid à une date, lecture seule (2026-09-25, hors comptage)** : `load_context_series` / `warmup_needs` / `indicator_requirements` de `backtest.py` appelés sans modification avec les arguments du moteur grid (`GRID_CONTEXT_WINDOWS`, `engine_loaders`, `T` recalculé par `cc.anchor_of`) ; classes `decision_timeframes` C1-C6 (`CLASSES`, `class_of`, oracle `bias_live` sur `_get_directional_bias`) ; D2 par classe × paire (`d2_cell`, `d2_table`) ; contrôles sourcés ; `ReadOnlyDatabaseManager` (`default_transaction_read_only = on`) ; JSON strict ; codes 0 / 3 / 2 | | tests `tests/test_scripts/test_warmup_at.py` (93, épinglés au moteur et au comportement) ; artefact `results/sol_d2_1w_modes/` |
| `backup_db.sh` · `restore_db.sh` | | Dump/restore Postgres (serveur) | | |

## Points d'attention transverses

1. **Defaults `"kraken"`** encore présents : `OHLCData.exchange:65`, `backtest.py` (4×), `feature_store.py:472`. `settings.exchange_name` est **obligatoire** depuis B1 (`EXCHANGE_NAME` absent → erreur explicite au démarrage) ; reste `deploy.yml` / `.env` serveur (B2).
2. **Kraken en dur hors connecteur** : `execution/order_manager.py:28` et `execution/risk.py:35` (`normalize_asset_*`), `engine.py:82` (type hint). ✅ B3 : `scheduler/task_scheduler.py` passe par le client de la factory et n'importe plus `scripts/` ; `fetch_ohlc.py` supprimé.
3. **`XBT/USDC`** : `settings.trading.pair:196`, `ScheduledTasksSettings:355`, `dashboard.py` (7), `kraken/ws.py:74-75` ; les stratégies utilisent `self.pair`.
4. **Registres statiques** : `main.STRATEGY_REGISTRY:71` (router seul) et `multi_strategy_router._INNER_STRATEGY_CLASSES:54` — une stratégie non listée est ignorée même si présente dans `strategies.yaml`.
5. ✅ B4.2 : **fees de backtest découplées de la source de données** — `--fees {bybit,binance,kraken}` obligatoire (`backtest.py`, runners P6/P7, walk-forward), `ExchangeFees.from_name()`, maker/taker par site de fill, taker aux liquidations forcées du `GridBacktester` (`skills/backtest.md`, `results/B4_2_fees_engine_report.md`). Reste B4.3 : `_force_close_open_positions` inopérant sur le chemin grok (`_last_close` jamais posé), double comptage de la fee de vente dans `net_pnl` (2 moteurs), sorties limit marketables facturées maker.
6. ✅ B3 : `exchange` obligatoire dans `multi_timeframe.py:236` et `multi_pair_registry.py:91` (plus de default `"binance"`). ✅ B4.1 : convention de timestamp unifiée en fin de période (rows `binance` re-stampées le 2026-09-13, `results/B4_1_timestamp_restamp_report.md`) ; les clients REST Binance/Kraken renvoient toujours l'open time ccxt (dette 12).
7. **Fichiers > 1 000 lignes** à ne pas toucher sans review : `kraken/rest.py`, `binance/rest.py`, `bybit/rest.py`, `kraken/ws.py`, `main.py`, `multi_timeframe.py`, `backtest.py`, `dashboard.py`.
8. ✅ B4.3 (mergé le 2026-09-15) : **moteur grid honnête** — liquidation terminale de l'inventaire atteignable sur le chemin grok (`GridBacktester._force_close_open_positions:2356`, MARKET au dernier close tradeable, taker + spread + slippage par paire, trades `forced_liquidation`, bloc `liquidation` dans les dumps/résultats), `net_pnl` = chaque fee comptée une fois dans les deux moteurs (grid : cash réalisé `ending − capital`) ; règle de flag `scripts/b4_flags.py` (divergence d'inventaire / résidu / `net_pnl ≠ net_pnl_lot_basis`) → **règle GO P7 n° 1** : config flaggée inéligible à la sélection (`p7_report.ineligible_configs:379`) ; **dette 13** : les moteurs résolvent les params des stratégies grok par nom de classe (`_load_inner_strategy_params:418`, `_load_grid_strategy_params:1750`) → défauts de classe, `effective_params` capturés au runtime ; **dette 14** : tolérance de fermeture absolue du grid (`|sell_level − prix| < 1` USD) → 48/48 configs SOL flaggées ; `LOG_LEVEL` non honoré par les scripts (`configure_logging` appelé seulement par `collector.py` / `main.py`). Campagne B4 : P6 0/24, P7 0/35, sélection paper vide (`results/B4_bybit_backtest_report.md`).
