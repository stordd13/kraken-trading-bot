# Rejeu de déterminisme serveur — porte pré-merge C2 (2026-09-19)

Preuves du rejeu des 24 tests `_full` sur le serveur, exécuté le 19/09/2026 avant le merge de C2
(RESEARCH_LOG, entrée « Porte pré-merge C2 »). Récupérées le 25/09/2026 depuis `~/c2-determinism/`
sur le serveur, où elles vivaient seules ; la recette d'archivage n'existait pas encore (adoptée avec C3a).
`junit/` = premier passage (`run24.sh`), `junit2/` = second passage (`run24b.sh`), `rest.*` = reliquat.
